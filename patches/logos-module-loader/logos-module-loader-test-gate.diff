diff --git a/CMakeLists.txt b/CMakeLists.txt
index cb77f11..c2445ae 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,6 +14,8 @@ set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
 # Build src first
 add_subdirectory(src)
 
+option(LOGOS_MODULE_LOADER_BUILD_TESTS "Build the test suite (needs GoogleTest)" ON)
+if(LOGOS_MODULE_LOADER_BUILD_TESTS)
 # GoogleTest setup
 find_package(GTest QUIET)
 
@@ -33,6 +35,7 @@ include(GoogleTest)
 
 # Add tests subdirectory
 add_subdirectory(tests)
+endif()  # LOGOS_MODULE_LOADER_BUILD_TESTS
 
 # Header-only contract: nothing to install but the headers.
 install(DIRECTORY src/logos_module_loader/ DESTINATION include/logos_module_loader
