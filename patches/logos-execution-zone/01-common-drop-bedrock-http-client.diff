Rationale: minimal form asked for in X3 - drop lez/common's only Bedrock edge (logos-blockchain-common-http-client, used only by From<BasicAuth> for BasicAuthCredentials) so wallet-ffi stops pulling Bedrock circuits/rapidsnark/GMP. BREAKS sequencer_core/indexer_core (they use that From impl); prefer 01b.

diff --git a/Cargo.lock b/Cargo.lock
index bc454464b..31b5763bf 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -1815,7 +1815,6 @@ dependencies = [
  "lee",
  "lee_core",
  "log",
- "logos-blockchain-common-http-client",
  "ping_core",
  "programs",
  "serde",
diff --git a/lez/common/Cargo.toml b/lez/common/Cargo.toml
index 449ad59c2..ad92ffa1f 100644
--- a/lez/common/Cargo.toml
+++ b/lez/common/Cargo.toml
@@ -29,7 +29,6 @@ sha2.workspace = true
 log.workspace = true
 hex.workspace = true
 borsh.workspace = true
-logos-blockchain-common-http-client.workspace = true
 
 [dev-dependencies]
 lee = { workspace = true, features = ["test-utils"] }
diff --git a/lez/common/src/config.rs b/lez/common/src/config.rs
index ec7ed6b38..385a90f49 100644
--- a/lez/common/src/config.rs
+++ b/lez/common/src/config.rs
@@ -2,7 +2,6 @@
 
 use std::str::FromStr;
 
-use logos_blockchain_common_http_client::BasicAuthCredentials;
 use serde::{Deserialize, Serialize};
 
 #[derive(Debug, Clone, Serialize, Deserialize)]
@@ -48,12 +47,6 @@ impl FromStr for BasicAuth {
     }
 }
 
-impl From<BasicAuth> for BasicAuthCredentials {
-    fn from(value: BasicAuth) -> Self {
-        Self::new(value.username, value.password)
-    }
-}
-
 #[cfg(test)]
 mod tests {
     use std::str::FromStr as _;
